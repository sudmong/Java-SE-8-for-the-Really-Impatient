package com.exercise.chap2;

import java.util.stream.Stream;

/**
 * Problem Statement
 * 
 * Write a method public static <T> Stream<T> zip(Stream<T> first, Stream<T> second)
that alternates elements from the streams first and second, stopping when
one of them runs out of elements.
 * @author Sudhir Mongia
 *
 */
public class Ex8TwoStreamTraversal {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}
	
	
	public static <T> Stream<T> zip(Stream<T> first, Stream<T> second)
	{
		Stream<T> result = null;
		
		//find the minimal length
		// for 1-len
		///print i from first and i+1 from second
		//second.
		
		//Stream.builder().accept(arg0);
		
		
		return result;
	}

}
